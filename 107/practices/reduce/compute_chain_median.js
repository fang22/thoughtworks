'use strict';

function compute_chain_median(collection) {
  for(var i = 0,len = collection.length;i < len;i++){
    collection.sort();
    console.log(collection)
    // if(len%2 != 0){
    //   var num = num.charAt(len/2)
    // }else{
    //   var num =
    // }
  }
}

module.exports = compute_chain_median;
